// eslint-disable-next-line @typescript-eslint/no-unused-vars
namespace Providers.<%= patternNamePC %>.<%= providerNamePC %>.Enum {
	
	/**
	 * <%= providerNamePC %> Events
	 */
	export enum Events {
		// TODO (by CreateNewPattern): Add Events reference
	}

	/**
	 * <%= providerNamePC %> Enum properties
	 */
	export enum Properties {
		// TODO (by CreateNewPattern): Add all pattern properties
	}
}
