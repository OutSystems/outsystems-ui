// eslint-disable-next-line @typescript-eslint/no-unused-vars
namespace OSFramework.Patterns.<%= patternNamePC %>.Enum {
	/**
	 * <%= patternNamePC %> Enum for CSS Classes
	 */
	export enum CssClass {
		// TODO (by CreateNewPattern): Add CssClasses values
	}

	/**
	 * <%= patternNamePC %> Events
	 */
	export enum Events {
		// TODO (by CreateNewPattern): Add Events reference
	}

	// <%= patternNamePC %> Mode Types
	export enum Mode {
		// TODO (by CreateNewPattern): Add mode types reference
	}

	/**
	 * <%= patternNamePC %> Enum properties
	 */
	export enum Properties {
		// TODO (by CreateNewPattern): Add all pattern properties
	}

	/**
	 * <%= patternNamePC %> Enum for Providers
	 */
	export enum Provider {
		// TODO (by CreateNewPattern): Check if provider has been properly added!
		<%= providerNamePC %> = '<%= providerNameLC %>',
	}
}
