# don't ever lint custom-scripts
custom-scripts
# don't ever lint node_modules
node_modules
# don't lint build output (make sure it's set to your correct build folder name)
dist
# don't ever lint gulp
gulp
# don't lint libraries
src/libraries/**
src/static/**
*.d.ts
*.js
# don't lint settings
*.snyk