// eslint-disable-next-line @typescript-eslint/no-unused-vars
namespace Providers.RangeSlider.Enum {
	export enum NoUISsliderLabels {
		Single = 'handle',
		Lower = 'lower-handle',
		Upper = 'upper-handle',
	}

	export enum NoUISliderEvents {
		Change = 'change',
		End = 'end',
		Start = 'start',
		Slide = 'slide',
	}

	export enum NoUiSliderConnectOptions {
		Lower = 'lower',
	}

	export enum NoUiSliderModeOptions {
		Count = 'count',
	}
}
