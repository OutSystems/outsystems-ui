// eslint-disable-next-line @typescript-eslint/no-unused-vars
namespace Providers.OSUI.RangeSlider.NoUiSlider.Enum {
	export enum ProviderInfo {
		Name = 'noUISlider',
		Version = '15.8.1',
	}

	export enum NoUISliderLabels {
		Handle = 'handler',
		Lower = 'lower-handle',
		Single = 'handle',
		Upper = 'upper-handle',
	}

	export enum NoUISliderEvents {
		Change = 'change',
		End = 'end',
		Start = 'start',
		Slide = 'slide',
	}

	export enum NoUiSliderConnectOptions {
		Lower = 'lower',
	}

	export enum NoUiSliderModeOptions {
		Values = 'values',
	}
}
