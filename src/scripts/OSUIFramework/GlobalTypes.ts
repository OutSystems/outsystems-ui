// eslint-disable-next-line @typescript-eslint/no-unused-vars
namespace OSUIFramework.GlobalTypes {
	// eslint-disable-next-line @typescript-eslint/no-unused-vars
	export type Orientation = {
		Horizontal: string;
		Vertical: string;
	};

	// eslint-disable-next-line @typescript-eslint/no-unused-vars
	export type Direction = {
		Left: string;
		Right: string;
	};
}
