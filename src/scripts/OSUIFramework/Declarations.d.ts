/**
 * Global cordova type definition
 */
export declare global {
	interface Window {
		cordova: any;
	}
}

/**
 * Global navigator.standalone type definition
 */
export declare global {
	interface Navigator {
		standalone: any;
	}
}
